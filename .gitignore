# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
build/
dist/

# Caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
*.env
.envrc

# Secrets / keys (defense in depth; no such files should exist in repo)
*.key
*.pem
*.p12
*.pfx
api_key*
*_secret*
secrets/
.secrets/
.anthropic/

# Claude Code local-user permission cache (machine-specific allow-list)
.claude/settings.local.json

# Local databases (dev runtime)
*.db
*.db-journal
*.sqlite
*.sqlite3

# Runtime logs (48h operational-shakedown output may include task
# text and prompt content; keep local, do not commit)
*.log
logs/

# Scratch / throwaway experiments (per CLAUDE.md convention:
# temporary experiments and large reference material Lewie drops
# in for in-session use; never intended as durable artifacts).
planning/scratch/
