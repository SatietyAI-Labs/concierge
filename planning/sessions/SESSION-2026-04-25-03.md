# Session SESSION-2026-04-25-03

**Started:** 2026-04-24 ~12:00 PDT (Fix Day 4 opener pasted; logical-day framing 2026-04-25 per today.md — same pattern as Fix Day 3 day-shift)
**Ended:** 2026-04-24 ~15:15 PDT
**Primary goal:** Fix Day 4 — narration-as-push + real-time surface + scanner + integration. Collaborative visibility via narration; SSE delivers new-request events; weekly scanner auto-promotes / flags; full integration cycle.
**Goal achieved:** Yes — all seven Fix Day 4 tasks shipped with tests, two scoped cuts documented explicitly (Task 4 streaming wire-format test deferred; loader-emit wiring deferred). Checkpoint criteria: 8 of 9 checkable-by-test green; the live-narration-in-fresh-Claude-Code-session criterion requires a manual shakedown run (noted as follow-up, not blocking ack of Fix Day 4).

## What was attempted this session

Standard open-phase cadence: read CLAUDE.md (already in context), ops-protocol, today.md, DECISIONS tail covering the five Fix Day 3 entries, prior SESSION-2026-04-25-02. Surfaced nine forks at session open (A–I) covering URI scheme, preamble inventory, side_observations categories, SSE emit site, scanner manual trigger, demotion persistence, ambiguous-case threshold, skills-specific detection, session_id semantic rule. Lewie accepted all defaults with two reversals: Fork C trimmed from four categories to two (retired-tool overlap + idle loaded-on-boot), Fork G moved from 14d to 30d install-age threshold. Fork H (skills detection) deferred as proposed.

Open-phase audit located the exact five Class-1 prompt-fragment constants (X3/X4/X6/X7-A/X8) + the adapter-authored gap-preamble, and discovered an existing URI-scheme precedent in `adapters/claude_code/meta_tools/gap_preamble.py:55-56` that contradicted my originally-proposed Fork A default. Surfaced the revision (`concierge://preamble/{name}` → `concierge://prompts/{name}.md`), Lewie greenlit the reversal, proceeded. Task 2 landed first per the revised sequence.

Session ran single-block per Fix Day 1/2/3 precedent. Task sequence followed the greenlit order: open-phase audit → Task 2 (resources) → Task 1 (tool-description enrichment) → Task 3 (side_observations) → Task 6 (session_id) → Task 4 (SSE) → Task 5 (scanner) → Task 7 (integration). Two mid-stream scope cuts:

1. **Task 4 streaming wire-format test deferred.** sse-starlette's long-lived pings + TestClient's sync iter_lines combination caused tests to hang without pytest-timeout installed. Rather than add the dep mid-session or spend 30min debugging async cancellation in TestClient, scoped the HTTP-byte test out and replaced it with an endpoint-registration test plus direct broker + service-publish coverage. Byte-level streaming verification lives in Task 7 integration (which does the full pipeline via direct calls, not HTTP) + deferred live-curl at session close as the operational check.

2. **Loader emit site stayed deferred.** Fix Day 3's session snapshot flagged that the loader `load()` emit doesn't exist (BackingServerRegistry has `register()` not `load()`, and no emit is wired). Today.md Task 6 said "three emit sites" but only two emit today. Task 6 shipped `SHIM_SESSION_ID` + wired propagation through recommend + install. When the loader emit eventually lands, the shim session id is importable for trivially-available-session-id semantics per Fork 2.

Closing live verification: the Task 7 integration test (`test_integration_full_cycle.py`) exercises create_request → broker publish → approve → install → usage event → scanner → promotion → identity refresh in one pass. Two variants: with memory wired (asserting identity_set fires) and without (scanner auto-promotes but identity refresh gracefully skips). Both green.

## What was completed

- **Task 2 — MCP resources protocol** — new `adapters/claude_code/resources.py` (~200 lines). Six `ResourceSpec` instances exposing X3/X4/X6/X7-A/X8 + gap-preamble verbatim at `concierge://prompts/{name}.md`. `register_resources(dispatcher)` declares the `resources` capability on `Dispatcher`, registers each spec, wires `resources/list` + `resources/read` method handlers. Dispatcher gained `ResourceSpec` dataclass, `_resources` + `_capabilities` fields, `register_resource` / `list_resources` / `resolve_resource` / `declare_capability` / `capabilities` methods. `_handle_initialize` refactored into a closure that reads `d.capabilities()` at dispatch time. Shim startup wires `register_resources` before stdin-read. `gap_preamble.py` docstring updated from "Deferred per N12 proposal Q4 answer" to "Implemented in Fix Day 4 Task 2" with implementation URI citation. 27 tests in `tests/test_mcp_resources.py` (byte-identity parametrized across all six URIs; error cases for unknown URI / missing uri / non-string uri / non-object params; idempotent re-registration; capability advertisement). 1 new test in `test_shim_dispatcher.py` for the declare_capability → initialize wire-through. `test_shim_e2e.py` initialize-test updated to expect `{"tools": {}, "resources": {}}`.

- **Task 1 — Narration requirement in tool descriptions** — `concierge_recommend` + `concierge_request_tool` descriptions both gained an "After invoking this tool, your next user-visible message must briefly narrate..." paragraph tailored per tool (recommend: what you asked, what it recommended, what you're doing; request: what gap you noticed, what tool you filed, confirming you're continuing). `concierge_list_active` intentionally NOT enriched (inventory-only, non-collaborative). 2 tests in `tests/test_meta_tools_register.py::TestNarrationRequirement` pin the "must briefly narrate" + "user-visible message" anchor phrases on the two collaborative tools, and pin that list_active stays silent on narration.

- **Task 3 — `side_observations` narration-as-push pattern 3** — `RecommendResponse` gained `side_observations: Optional[list[str]]`. `JSON_OUTPUT_ENVELOPE` in `core/recommend/prompt.py` grew a §side_observations section (~50 lines) instructing Opus on exactly two trigger categories per Fork C's trim: (1) retired-tool overlap with the task, (2) idle loaded-on-boot tool whose category matches the task domain. Prompt explicitly permits silence ("omit the key or return []", "silence is correct") and caps at two observations. Parser extended to 3-tuple return `(reasoning, recommendations, side_observations)`; non-list / non-string-entries raise RecommendationParseError. Service threads through to RecommendResponse. Renderer adds conditional `### Observations` section between Gap report and Summary; absent / empty / null collapses the block (byte-identical to pre-Task-3 rendering). Validator's `_check_side_observations` — absent-or-null is not drift; present-wrong-type / > 2 entries / non-string item IS drift. ~27 tests across parse (6), prompt (5), validator (7), render (7), schema (2).

- **Task 6 — session_id propagation** — `UsageEventSink` signature extended from `(slug, event_type, context)` to `(slug, event_type, context, session_id)`. `noop_sink` + `make_db_sink` both accept the new kwarg; `emit_usage_event` already had it since Fix Day 3. `RecommendRequest.session_id: Optional[str]` added; recommend service threads through to sink call. `StatusChange.session_id: Optional[str]` added; `update_status` passes to `_maybe_install_on_approve(..., session_id=)` which threads to the installed-event emit. New `adapters/claude_code/session.py` with `SHIM_SESSION_ID = str(uuid.uuid4())` minted at module import. Meta-tool `recommend._build_request_body` reads `_shim_session.SHIM_SESSION_ID` at call time (monkeypatchable) and attaches to every POST /recommend body. 7 tests (3 in test_shim_session, 2 in test_recommend_service session-propagation, 1 in test_lifecycle_service session-propagation, 1 in test_meta_tools_recommend session-in-body). `_CapturingSink` in test_recommend_service extended to the 4-tuple shape; existing tests updated with `(slug, event, ctx, _session_id)` unpack where they only care about the first three. Loader-emit stayed deferred (no BackingServerRegistry.register emit wired today).

- **Task 4 — SSE real-time surface** — new `core/events.py::EventBroker` (~130 lines). In-process asyncio.Queue fan-out; subscribe() is an async generator that registers a queue and cleans up on `finally`; publish() is sync (put_nowait + swallow QueueFull) so service code can emit without `await`. New `core/api/events.py` with `GET /ui/events` returning `sse_starlette.sse.EventSourceResponse(broker.subscribe())`. `LifecycleService.event_broker: Optional[EventBroker] = None` field; `create_request` publishes `new_request` event with `{filename, tool_name, tool_slug, category, confidence, is_discovered}` payload AFTER the DB commit. `core/app.py` lifespan creates `app.state.event_broker = EventBroker()` for the singleton-per-app shape. `core/api/requests.py::get_event_broker` + `get_lifecycle_service` wire it via FastAPI DI, defensively tolerant of missing app.state (for legacy test fixtures that construct TestClient without context manager). `sse-starlette>=2.0` added to pyproject.toml dependencies. 11 tests (7 in test_events broker-unit, 4 in test_ui_events_endpoint: route-registered + service-wiring three variants including field-shape pin + no-broker fallback).

  Scoped cut: the streaming HTTP byte-format assertion (e.g. "actually seeing `event: new_request` on the wire") is NOT tested in this file. sse-starlette's long-lived connection + TestClient's sync iter_lines + no pytest-timeout plugin combination made tests hang. The byte-level wire format is verified indirectly via Task 7's integration test (which reads events through the broker directly) + reserved for manual curl at live verification.

- **Task 5 — Promotion/demotion scanner** — new `core/lifecycle_scanner.py` (~330 lines). `run_once(session, *, memory=None, now=None) -> ScannerSummary`. Aggregates `tool_usage_events` in the `PROMOTION_SIGNAL_EVENT_TYPES = {"recommended", "loaded", "used"}` set over `PROMOTION_WINDOW_DAYS` (30). Tools meeting `PROMOTION_MIN_USES` (5) are candidates; classification function returns one of `auto_promoted` / `install_age_below_threshold` / `already_loaded_on_boot` / `retired_cannot_promote`. Fork G: auto-promote only when install age ≥ 30 days (`AUTO_PROMOTE_MIN_INSTALL_AGE_DAYS`). Calls `transition_tool_lifecycle(...)` with `refresh_identity_on_loaded_on_boot_change` as the on_transition hook (Fix Day 3 Fork 5 wire-in finally lighting up). Demotion flagging: loaded-on-boot tools with zero events (sentinel inactivity_days=-1) or events older than `DEMOTION_INACTIVITY_DAYS` (90d); flag-only per Fork F (no auto-demote). Stale pending: `requests.folder='pending' AND created_at < now-7d`. Summary includes a `to_health_dict()` projection. New `core/api/scanner.py` with `POST /scanner/run` (Fork E — authless local-only manual trigger). APScheduler `AsyncIOScheduler` weekly job registered in `core/app.py` lifespan (Sunday 03:00 local cron trigger, `_run_scheduled_scan` opens its own session). `GET /health` augmented with a `scanner` field reading from `app.state.last_scanner_summary` (Fork F: null before any run, populated after). `apscheduler>=3.10` added to deps. 26 tests (21 in test_lifecycle_scanner covering threshold / classification / demotion / stale-pending / to_health_dict shape; 5 in test_scanner_endpoint covering empty-db + auto-promote + app.state summary + /health null-before-first-run + /health populated-after).

- **Task 7 — End-to-end integration** — new `test_integration_full_cycle.py` (2 tests, `@pytest.mark.asyncio`). `test_request_event_install_scanner_promotion_identity` walks the full chain: create_request → broker.new_request → approve → install dispatcher (stubbed) → ToolUsageEvent(installed) with session_id → seed 5 recommended events + nudge catalog created_at to simulate 60-day install age → `run_once(db, memory=FakeMemory, now=now)` → auto-promotion fires → identity_set called on FakeMemory → identity text contains the newly-promoted slug. Second test `test_cycle_survives_no_memory_client` exercises memory=None graceful-degradation: auto-promote still fires, identity refresh silently skipped. Both green.

- **Defensive fixture tolerance** — `get_event_broker` falls back to None when `app.state.event_broker` is unset. Catches legacy test fixtures constructing `TestClient(app)` without entering the context manager (which skips lifespan and leaves `app.state` empty). Prevents the smoke-endpoints suite regression that surfaced on the first Task 4 full-suite run.

- **`_StaticMemory` stub in `test_smoke_live_anthropic.py` gained identity_get** — tracked down from Fix Day 3's miss (session snapshot said "3 FakeMemory stand-ins updated" but this fourth live-smoke-marked stub was missed).

- **Tests:** 625 at session start → **751 passing, 1 skipped, 1 deselected** at session close (m="not live_smoke"). +126 tests in fast suite. Zero regressions.

## What was started but not finished

Nothing mid-flight. All seven Fix Day 4 tasks complete. Deferred items are explicit and captured:

- **Task 4 streaming HTTP byte-format test.** Deferred to Task 7 integration (which tests the pipeline via direct broker subscribe, not HTTP streaming) + manual curl at live verification. If future soak reveals sse-starlette drift, add `pytest-timeout` dep and write a properly-cancellable httpx AsyncClient test. Alternative: an ASGI-LIFESPAN harness that gives reliable streaming close semantics.

- **Loader-emit wiring.** `BackingServerRegistry.register()` doesn't emit a `loaded` ToolUsageEvent today. The Fix Day 3 snapshot flagged this; Fix Day 4 Task 6 made the session_id source available (`SHIM_SESSION_ID`) but did not land the emit. Next logical slot: the V1-with-actual-backing-servers phase, where the registry starts registering real servers and the emit gains real signal.

- **Live narration verification.** Checkpoint criterion "In a fresh Claude Code session, invoking `concierge_recommend` produces a user-visible message narrating the consultation" requires a live MCP-client-driven session which can't be scripted from the build harness. The tool-description anchor phrase is pinned by tests; the full user-visible narration requires a real Claude Code client as the reader. Operator runs this as part of the Day 5+ 48h shakedown.

## Decisions made this session

Six formal DECISIONS entries appended to `planning/decisions/DECISIONS.md` (per Lewie's close-out instruction to document at least Fork A, B, C, D, G, I):

- **Fork A — `concierge://prompts/{name}.md` URI scheme for MCP resources.** Documents the originally-proposed `concierge://preamble/{name}` default, the audit-discovered precedent in `gap_preamble.py:55-56`, the reversal, and the bonus finding that Task 2 closed a documented deferral rather than introduced a new surface.
- **Fork B — Six-resource preamble inventory** (X3 tool-awareness, X4 tool-recommendation, X6 tool-discovery, X7-A tool-lifecycle-weekly-review, X8 behavioral-rules, gap-preamble). Pins the set so future "should we add X9" discussions have a starting point.
- **Fork C — side_observations trigger categories: retired-tool overlap + idle loaded-on-boot.** Two strong-signal categories, not four mixed-signal. Documents the two rejected categories (b adjacent-pack-member, d catalog-gap-for-request) and Lewie's reasoning.
- **Fork D — Service-layer SSE emit site.** `LifecycleService.create_request` publishes via the broker; endpoint layer is consumption-only. Captures the "any non-HTTP filing path eventually feeds the same broker" reasoning.
- **Fork G — 30-day install-age threshold for auto-promotion.** Chose 30d over 14d; trigger-happy promotion is the worse failure mode; demotion-by-missed-signal is recoverable via re-use patterns. Documents the asymmetric-recoverability argument.
- **Fork I — session_id semantic rule.** MCP-originated emits carry `SHIM_SESSION_ID`; UI-originated emits pass null. The field's meaning is caller-context, not data-context; partial population is the honest shape.

## Bugs / issues discovered

One self-inflicted + one pre-existing:

- **Task 4 streaming test hang (self-inflicted).** Initial streaming test used `TestClient.stream(...)` + sync `iter_lines` + `break`, assuming break would propagate cancellation to the server-side generator. sse-starlette's long-lived connection + TestClient's thread-based event loop made this hang indefinitely. Attempted rewrite with httpx.AsyncClient + ASGITransport still hung (ASGITransport doesn't run lifespan, and the streaming coroutine needs pytest-timeout-style hard bounds). Resolution: scope cut — verify the pipeline via direct broker subscribe (Task 7 integration) + manual curl at live-verify.

- **`_StaticMemory` missed during Fix Day 3 identity wiring (pre-existing).** Fix Day 3's session snapshot said "3 existing FakeMemory stand-ins updated to expose `identity_get`". The `_StaticMemory` class in `tests/test_smoke_live_anthropic.py` (live-smoke-marked, deselected from default runs) was the fourth — missed at the time, surfaced on my first full-suite regression sweep. Fixed here (stub now returns "" on identity_get, matching the FakeMemory pattern).

## Open questions for Lewie — resolved at post-session review

All four open questions answered during the same review that greenlit the six DECISIONS entries. Resolutions captured here for the next session's reading:

1. **Live narration verification** — Does NOT block code commit; DOES block Fix Day 4 ack-as-complete. Plan a manual live-shakedown run in a fresh Claude Code session post-commit (~20-30 min). Same Appendix B transcript pattern as Fix Days 1-3. Capture: did `concierge_recommend` produce visible narration in the agent's response? Append transcript to this SESSION-2026-04-25-03.md as Appendix B before declaring Fix Day 4 fully closed. **Next-session action required.**

2. **Loader-emit wiring scheduling** — Deferred until the first real backing-server registration. Capability is ready today (`SHIM_SESSION_ID` available via `adapters/claude_code/session.py`, `ToolUsageEvent.session_id` accepts the string, scanner aggregates `loaded` events as promotion signal). Wire-up cost when the trigger event arrives is ~5 min: add one `emit_usage_event(..., event_type="loaded", session_id=SHIM_SESSION_ID)` call inside `BackingServerRegistry.register()` after the server starts successfully. Do not add code for a hypothetical caller tonight.

3. **Task 4 streaming test resurrection** — Worth the `pytest-timeout` dep, scheduled for Day 5 morning (not pre-commit). The HTTP-over-wire SSE test is a separate failure mode from broker-direct (sse-starlette framing changes, MCP/HTMX expectation drift). **Day 5 today.md: first task of the day — add pytest-timeout dep + write properly-cancellable streaming test asserting `event: new_request\ndata: {json}` byte format on the /ui/events wire.**

4. **Scanner cadence** — Daily during shakedown, revert to weekly post-soak. The 48h soak window is the whole point of running the scanner; weekly means it fires once or twice during shakedown which gives almost no signal. Changed `core/app.py` lifespan before commit: `CronTrigger(hour=3, minute=0)` fires nightly at 03:00 local. Comment in the changed line names this as shakedown cadence. **Day 5 today.md: checklist item to revert `CronTrigger(hour=3, minute=0)` → `CronTrigger(day_of_week="sun", hour=3, minute=0)` at end-of-soak.**

## Next session should start by

Either (a) open a fresh Claude Code session against a running uvicorn + configured concierge-shim for live-narration verification, or (b) move to UI Day per the close-the-gap plan. Lewie's call — Fix Day 4 substantively complete; UI Day is next per plan, but operator may want a live-shakedown pass first.

Main on commit `7048da4` + uncommitted Fix Day 4 work. Working tree: 28 modified files, 11 new files, ~1100 lines. Full suite 751 passing (m="not live_smoke"); zero regressions.

## Context the next session needs to read first

1. `planning/sessions/SESSION-2026-04-25-03.md` — this snapshot
2. `planning/today.md` — Fix Day 4 plan (still authoritative until UI Day's today.md is written)
3. `docs/close-the-gap-plan-2026-04-23.md` §UI Day section (if moving to UI)
4. `planning/decisions/DECISIONS.md` tail — six new Fix Day 4 entries (Forks A, B, C, D, G, I)
5. `core/lifecycle_scanner.py` + `core/events.py` + `adapters/claude_code/resources.py` — the three new substantive surfaces

---

## Appendix A — File-level delta this session

**New source modules:**
- `adapters/claude_code/resources.py` (~200 lines, Task 2)
- `adapters/claude_code/session.py` (~30 lines, Task 6)
- `core/events.py` (~135 lines, Task 4)
- `core/api/events.py` (~80 lines, Task 4)
- `core/lifecycle_scanner.py` (~330 lines, Task 5)
- `core/api/scanner.py` (~70 lines, Task 5)

**New test modules:**
- `tests/test_mcp_resources.py` (27 tests, Task 2)
- `tests/test_events.py` (7 tests, Task 4)
- `tests/test_ui_events_endpoint.py` (4 tests, Task 4)
- `tests/test_shim_session.py` (3 tests, Task 6)
- `tests/test_lifecycle_scanner.py` (21 tests, Task 5)
- `tests/test_scanner_endpoint.py` (5 tests, Task 5)
- `tests/test_integration_full_cycle.py` (2 tests, Task 7)

**Modified source:**
- `adapters/claude_code/dispatcher.py` — `ResourceSpec`, `_resources`, `_capabilities`, `declare_capability`, refactored `_make_handle_initialize`
- `adapters/claude_code/shim.py` — wires `register_resources(dispatcher)`
- `adapters/claude_code/meta_tools/gap_preamble.py` — docstring updated: deferral → implemented
- `adapters/claude_code/meta_tools/recommend.py` — narration description, session_id body inclusion
- `adapters/claude_code/meta_tools/request_tool.py` — narration description
- `adapters/claude_code/meta_tools/render.py` — `### Observations` section (conditional)
- `core/app.py` — EventBroker lifespan wiring, APScheduler, scanner scheduled job, teardown
- `core/api/health.py` — scanner field from `app.state.last_scanner_summary`
- `core/api/requests.py` — `get_event_broker` defensive, `get_lifecycle_service` passes broker
- `core/lifecycle_store/schema.py` — `StatusChange.session_id`
- `core/lifecycle_store/service.py` — `event_broker` field, publish from `create_request`, session_id thread-through install path
- `core/recommend/prompt.py` — JSON_OUTPUT_ENVELOPE §side_observations
- `core/recommend/schemas.py` — `RecommendRequest.session_id`, `RecommendResponse.side_observations`
- `core/recommend/service.py` — 3-tuple parse, session_id in emit sink, side_observations in response
- `core/recommend/parse.py` — 3-tuple return + `_parse_side_observations`
- `core/recommend/validator.py` — `_check_side_observations`
- `core/telemetry.py` — `UsageEventSink` 4-arg signature, `noop_sink` + `make_db_sink` accept session_id

**Modified tests:** test_lifecycle_service.py, test_meta_tools_recommend.py, test_meta_tools_register.py, test_recommend_parse.py, test_recommend_prompt.py, test_recommend_service.py, test_recommend_validator.py, test_shim_dispatcher.py, test_shim_e2e.py, test_smoke_live_anthropic.py

**Planning:**
- `planning/sessions/SESSION-2026-04-25-03.md` (this file, new)
- `planning/decisions/DECISIONS.md` (six new entries appended)

**Deps:**
- `sse-starlette>=2.0` added
- `apscheduler>=3.10` added

## Appendix B — Task-level test counts

| Task | New tests | Modules |
|---|---|---|
| Task 2 (resources) | 28 | test_mcp_resources.py (27), test_shim_dispatcher.py (+1) |
| Task 1 (narration descriptions) | 2 | test_meta_tools_register.py |
| Task 3 (side_observations) | ~25 | test_recommend_parse.py, test_recommend_prompt.py, test_recommend_validator.py, test_meta_tools_recommend.py |
| Task 6 (session_id propagation) | 7 | test_shim_session.py (3), test_recommend_service.py (+2), test_lifecycle_service.py (+1), test_meta_tools_recommend.py (+1) |
| Task 4 (SSE broker + endpoint + wire) | 11 | test_events.py (7), test_ui_events_endpoint.py (4) |
| Task 5 (scanner) | 26 | test_lifecycle_scanner.py (21), test_scanner_endpoint.py (5) |
| Task 7 (integration) | 2 | test_integration_full_cycle.py |
| **Total** | **~101** | 7 new test files + 6 modified |

Suite: 625 → 751 (+126). Minor variance from rough per-task counts due to updated-not-added existing tests.

## Appendix C — Deferred items carrying into Fix Day 4 close-out

Non-blockers for code commit. Day 5 today.md should pick up the three marked ⟶ items first.

1. **Live narration verification** — ⟶ blocks Fix Day 4 ack-as-complete. Post-commit manual run in fresh Claude Code session; capture transcript to this snapshot's (forthcoming) Appendix B.
2. **Day 5 streaming-test resurrection** — ⟶ first task of Day 5. Add `pytest-timeout` dep; write cancellable httpx AsyncClient test asserting `event: new_request\ndata: {json}` byte format on `/ui/events`.
3. **Day 5 scanner-cadence revert** — ⟶ end-of-soak checklist. Revert `CronTrigger(hour=3, minute=0)` in `core/app.py` back to `CronTrigger(day_of_week="sun", hour=3, minute=0)` once shakedown gate clears.
4. Loader-emit wiring — ~5 min when first real backing-server registration arrives (see Open Question 2).
5. Scanner skills-specific "used" detection (Fork H; separate design pass).
6. PEP-668 install-strategy ruling (Fix Day 1 still-open).
