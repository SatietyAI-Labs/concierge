# Session SESSION-2026-04-24-01

**Started:** 2026-04-23 ~13:15 PDT (Fix Day 1 opener pasted; logical-day framing 2026-04-24 per close-the-gap-plan and today.md — work ran earlier than planned)
**Ended:** 2026-04-23 ~17:00 PDT
**Primary goal:** Fix Day 1 — catalog can express MCP / CLI / HTTP categories as peers; rich in-chat content schema in place; approve-triggers-install works end-to-end; memory denial-recall empirically verified.
**Goal achieved:** Yes — all six Fix Day 1 checkpoint criteria met + one architectural-bootstrap (Alembic) that was implicit in the plan's "migration" language. End-to-end live install verified against real uvicorn + real subprocess. Denial-recall verification: PASS with strong signal.

## What was attempted this session

Standard open-phase cadence: read the seven opener documents, verify the six `[2026-04-23]` DECISIONS landed cleanly at DECISIONS.md tail (yes), surface proposed defaults on seven ambiguity points from the plan (source of truth for ingest, idempotency semantics, validator strictness, X13 install-method source, dry-run definition, denial seeding, session structure), receive confirmation to proceed with judgment on all seven.

Session progressed through T1→T6 in a single block per Lewie's green-light, with two architectural pauses surfaced mid-stream: (a) Alembic not installed in the repo — chose Option A (bootstrap Alembic, remove `Base.metadata.create_all()` from lifespan in favor of programmatic `alembic upgrade head`); (b) test fixtures would trigger real pip/npm subprocesses under the T4 wire-in — chose DI-on-LifecycleService so three shared fixtures inject noops.

T6 denial-recall verification ran a live Opus call after seeding a synthetic denial in `~/.concierge-memory/`. Opus surfaced the denial from memory and cited it explicitly in both top-level reasoning and per-recommendation rationales. Cleanup of the synthetic entry succeeded via direct ChromaDB delete. Closing live end-to-end install verification (post-Lewie-review) produced a real pipeline execution with a PEP-668-surfaced install failure correctly captured by the Install section + auto-transition to `failed`.

## What was completed

- **Alembic bootstrap** — `pyproject.toml` adds `alembic>=1.13`; `alembic/env.py` wires to `core.config.Settings.database_path` with `render_as_batch=True` for SQLite and `compare_type=True` for autogen; `alembic.ini` URL fallback; baseline migration `4ff5b5898f71_baseline_existing_schema.py` (zero ops — live DB matched metadata exactly); `alembic stamp head` marked live DB at baseline.
- **`ensure_schema_current()` replaces `init_db()` in lifespan** — `core/db/session.py:45-63`; `core/app.py:9,21`. `init_db()` kept as test-only per-test-speed fast path.
- **T1: `tool_type` enum migration applied** — `core/db/models.py:12-20, 44-48` adds `TOOL_TYPE_VALUES` constant + `tool_type` column; migration `7bdb4433afa8_add_tool_type_enum_and_backfill.py` with backfill derived from `install_method` (mcp-server → mcp; apt / pip-user → cli); 4 rows backfilled, zero NULLs. `GET /tools` filter + response now include `tool_type` (`core/api/tools.py`, `core/api/schemas.py`).
- **T2: catalog ingest parser** — new `core/ingest/catalog.py` parsing MCP / CLI / HTTP sections from `_legacy/toolconcierge/TOOL-CATALOG.md`; idempotent upsert-by-slug preserving lifecycle fields; 12 unit tests at `tests/test_ingest_catalog.py` (11 parser + 1 real-source smoke). One live run populated +6 packs and +44 tools. Final catalog: cli=32, mcp=9, http=7, skill=0 (skill is Fix Day 2).
- **T3: rich in-chat content** — `ToolRecommendation` gains `category` / `install_method` / `risk_cost` as Optional fields (`core/recommend/schemas.py:79-107`); `CatalogToolView` adds `tool_type` + `install_method` so Opus can copy from catalog render (`core/recommend/prompt.py:142-166`); `JSON_OUTPUT_ENVELOPE` instructs explicit-null semantics (`prompt.py:80-138`); Tier 1 validator check `_check_rich_content_fields` added (`core/recommend/validator.py:124-146, 246-263`); 4 new tests in `TestValidatorRichContentDrift`; `core/api/recommend.py` catalog-view factory passes new fields through.
- **T4: X13 wire-in** — `_maybe_install_on_approve` post-hook in `core/lifecycle_store/service.py:384-458`; `update_install_section` writer primitive in `core/lifecycle_store/writer.py:185-229` (parse-modify-re-render for round-trip-parse-verify preservation); `install_dispatcher` DI field on `LifecycleService` defaulting to real `install_by_method`. Three shared test fixtures patched with noop dispatcher (`test_lifecycle_service.py`, `test_smoke_roundtrip.py`, `test_requests_api.py`). New `TestApproveTriggersInstall` with 4 tests (success / failure / non-canonical / exception paths).
- **T5: `install_npx_mcp`** — new installer at `core/install/methods.py:57-77` using `npm view <package> name` as the registry-check "install" (live prefetch deferred to soak); `METHOD_NPX_MCP = "npx_mcp"` constant; `normalize_install_method` routes `npx`-substring to this method; `install_by_method` dispatches it; 3 new unit tests for the installer + 5 new normalizer parametrizations + 1 dispatcher route test. `core/install/__init__.py` exports `install_npx_mcp`.
- **T6: denial-recall verification script + PASS result** — `scripts/verify_denial_recall.py` (gated by `CONCIERGE_LIVE_SMOKE=1`); seeds synthetic `tool-selection`+`denied` memory, issues live `/recommend`, asserts verdict, cleans up. Run once against real Opus with memory cold-start. VERDICT: **PASS — htop was not recommended; Opus explicitly acknowledged the prior denial.** Full transcript preserved below.
- **Live end-to-end install verification** — POST /requests (csvkit pip-user) → POST /requests/{filename}/status approved → dispatcher ran real `pip install --user csvkit` → exit=1 (PEP 668) → Install section written with command + verification + date → auto-transition to `failed`. Total elapsed **0.92s**. `/health` counters show `transitioned: 2` confirming the double-transition. Transcript below.
- **Tests:** 499 at session start → **528 passing, 19 deselected** at session close. Zero regressions across all six tasks + Alembic bootstrap + DI refactor.
- **Three new DECISIONS entries** appended at DECISIONS.md tail for the mid-session architectural calls: Alembic-owns-schema, rich-content-validator-WARN, install_dispatcher-DI.

## What was started but not finished

Nothing mid-flight. All six tasks complete with tests + verification. Deferred items are explicit:

- **Alembic migration-drift integration test** — queued as Fix Day 2 Task 0 (~30-45 min). Exercises Alembic empty → head to guarantee test-path (`Base.metadata.create_all()`) and production-path (`alembic upgrade head`) stay in sync. Per the `init_db()` test-only docstring note.
- **Rich-content validator Tier 1 → Tier 0 promotion** — deferred to soak. If 50+ clean real-Opus responses accrue with the three fields always present, a future decision can promote to hard-assert.
- **PEP-668 install strategy** — surfaced by live csvkit install (exit=1, externally-managed-environment). Need a design call on: `pipx install` vs system `pip3 --user` with explicit path vs `--break-system-packages` override vs operator escalation. Open question below.

## Decisions made this session

Three, all appended to DECISIONS.md in the Fix Day 1 block:

- Alembic owns schema; `create_all()` removed from startup — see DECISIONS.md `[2026-04-24 Fix Day 1]`
- Rich in-chat content validator is Tier 1 WARN, not Tier 0 hard-assert — see DECISIONS.md `[2026-04-24 Fix Day 1]`
- `install_dispatcher` is dependency-injected on `LifecycleService` — see DECISIONS.md `[2026-04-24 Fix Day 1]`

Each entry includes context, options considered, reasoning, reversibility (all Easy), and affects-list. The six `[2026-04-23]` decisions from the prior planning chat remain authoritative; these three are the mid-session architectural calls surfaced during Fix Day 1 implementation.

## Bugs / issues discovered

**None structural.** One real-world packaging issue surfaced via the live install verification but it is a system-environment concern, not a Concierge bug:

- **PEP 668 externally-managed-environment on Python 3.12.** `pip install --user csvkit` invoked via the venv's pip against Ubuntu 24.04's Python 3.12 returns exit=1 with `error: externally-managed-environment`. The wire-in correctly captured and surfaced this (Install section + status=failed), which is the right behavior — the failure is informational, not masked. Open question below covers the design response.

- **Exit code 144 from background-task uvicorn stops** is cosmetic. `pkill -TERM` correctly shuts uvicorn; the 144 signal-code surfaces from the background-task runner noticing the killed process. Not affecting the wire-in or tests.

## Open questions for Lewie

1. **PEP-668 install-strategy design.** Options: (a) switch `install_pip_user` to `pipx install` (PEP-668-compatible, isolated venvs per package); (b) invoke `pip3 install --user` against system python3 explicitly (uses `/home/satiety/.local/`, which is not PEP-668-locked); (c) add `--break-system-packages` to the current venv-pip path (explicit override, less safe); (d) accept failures for `pip-user` requests on PEP-668-locked systems and document operator escalation. Recommended: (a) for new installs + document (d) as the fallback. Default if no answer: (d) — current behavior is correct from an operational-first view (honest failure surfaces to operator); a Phase-2 switch to pipx can land after the Settings UI arrives.

2. **Promote rich-content validator from Tier 1 WARN to Tier 0 hard-assert when?** Suggested trigger: 50+ consecutive real-Opus recommendations during soak with all three fields present on every recommendation. Decision can be deferred — current Tier 1 behavior is non-blocking.

3. **Live-install verification cadence.** Lewie asked for a one-time live verification before closeout (done). Worth making the verification a repeatable script at `scripts/verify_approve_install.py` (mirroring the denial-recall pattern) so soak-phase runs can re-confirm the wire-in monthly? Default if no answer: yes, add to Fix Day 2 or soak-day backlog.

## Next session should start by

Open a fresh Claude Code session tomorrow morning (2026-04-25, Saturday — Fix Day 2). Paste the opener from `planning/today.md` §Session opener, which instructs the new session to read the seven Fix Day 2 documents (CLAUDE.md v3, ops protocol, blueprint-v2, close-the-gap plan, this snapshot, Fix Day 2's today.md, the prior Fix Day 1 decisions) and report its reading before touching code. First concrete action: Task 0 (migration-drift integration test, ~30-45 min) before moving to the sized Fix Day 2 tasks (skills ingest + tool-lifecycle schema).

Uvicorn (Terminal A, background task `b5quqazrl`) is warm with all T1-T5 changes loaded. `/health` shows `tools: 48, packs: 8`. Memory store is clean (synthetic denial removed). DB is at head revision `7bdb4433afa8`.

## Context the next session needs to read first

1. `planning/today.md` — Fix Day 2 plan with opener
2. `docs/close-the-gap-plan-2026-04-23.md` §Fix Day 2 section
3. `planning/sessions/SESSION-2026-04-24-01.md` — this snapshot
4. `planning/decisions/DECISIONS.md` tail — three new `[2026-04-24 Fix Day 1]` entries
5. `alembic/env.py` + `alembic/versions/` — Alembic setup (referenced by Task 0 migration-drift test)

---

## Appendix A — Denial-recall verification transcript

Run: `CONCIERGE_LIVE_SMOKE=1 python scripts/verify_denial_recall.py`
Memory dir: `/home/satiety/.concierge-memory/`
Uvicorn: `127.0.0.1:8000` (all T1-T5 code loaded)

**Seeded denial (tool_name=htop, tags=tool-selection+denied+verification-synthetic):**

> Tool selection decision: DENIED. Considered htop for agent process-monitoring tasks. Rejected in favor of existing `ps`, `top`, and Python's built-in `psutil` which are already available — htop adds a managed tool for marginal capability gain, and the lightweight-first preference rules out the addition. Do not recommend htop for monitoring tasks again unless the task materially requires interactive TUI features the built-ins cannot provide.

**Task issued via POST /recommend:**

> Monitor resource usage of a running Python agent process over time.

**Opus response (request_id `70d97c3c-0bd2-4e56-8f94-ad7f5214b653`):**

Top-level reasoning:

> The task is to monitor resource usage (CPU, memory, I/O) of a running Python process over time. **Memory explicitly records a prior DENIAL of htop for this exact task pattern**, with the stated preference for lightweight built-ins (ps, top, psutil). **I must respect that decision.** The best approach is to use already-active tools: bash with ps/top for sampling, and python3 with psutil for programmatic time-series capture. No interactive TUI is required by the task, so the denial still applies.

Recommendations (all 3 with rich in-chat content populated):

- **rank=1 `bash` (in_catalog=True, category=shell, install_method=null, risk_cost="no install; negligible runtime cost")** — "Already active and sufficient: a simple loop invoking `ps -p <pid> -o %cpu,%mem,rss,vsz,etime` (or `top -b -n1 -p <pid>`) on an interval produces a time-series of resource usage with zero install cost. **Matches the prior lightweight-first decision recorded in memory.**"
- **rank=2 `python3` (in_catalog=True, category=runtime, install_method=null, risk_cost="psutil may need `pip install --user psutil` if not present; otherwise no cost")** — "For structured, longer-running monitoring, a short python3 script using the stdlib plus psutil gives per-process CPU%, RSS, num_threads, io_counters, and open_files over time, and can emit CSV/JSON for later analysis. Python3 is already active; psutil is the standard library-style helper **the prior memory explicitly endorsed.**"
- **rank=3 `jq` (in_catalog=True, category=data-processing, install_method=null, risk_cost="no install; negligible runtime cost")** — "Complementary: if the python3 sampler emits JSON lines, jq (already active) is the natural post-processor for filtering/aggregating the time-series without pulling in heavier data tools."

**VERDICT:** `PASS — htop was not recommended.`

Stronger than the minimum-PASS bar: Opus not only avoided the denied tool, but explicitly **cited the memory-surfaced denial** in its top-level reasoning *and* referenced the prior decision in per-recommendation rationales. This is strong empirical validation of the DECISIONS `[2026-04-23]` A2 five-check-collapse decision — denial-recall is preserved via memory retrieval + X3/X4 prompt instruction without needing an explicit resolved-requests query branch in Python.

Synthetic memory cleaned up: `mem_2c6faec9d693` deleted via direct ChromaDB `col.delete(ids=...)`.

---

## Appendix B — Live end-to-end install verification

Run: curl POST /requests + POST /requests/{filename}/status via `scripts/concierge-shim`-less direct HTTP.
Uvicorn: `127.0.0.1:8000` (real `install_by_method` dispatcher, not noop).
Target: csvkit via install_method=pip-user against Python 3.12.3 on WSL2 Ubuntu 24.04.

**Pre-state:**
- `pip show csvkit` in venv: not found
- `~/.local/bin/csvstat` + `csvcut` present (prior system-pip3 --user install against system python3)

**Timeline:**

```
STEP 1: POST /requests → filename=2026-04-23-1649-csvkit.md, status=pending, id=6 (written)
STEP 2: pending/ file present, 586 bytes, no Install section
STEP 3: POST /requests/{filename}/status {"status":"approved"} → returns after 0.92s
   response status: "failed"
   response raw_markdown contains full Install section
STEP 4: final file contents = (below)
STEP 5: /health counters: lifecycle.transitioned = 2 (correct: pending→approved→failed)
```

**Final file (pending/2026-04-23-1649-csvkit.md, 836 bytes):**

```
status: failed

# Tool Request: csvkit

## Request

- **Task context:** Fix Day 1 live verification of approve-triggers-install X13 wire-in.
- **Tool suggested:** csvkit
- **Category:** data-processing
- **Install method:** pip-user
- **Discovered:** false

## Recommendation

- **Why this tool:** Lightweight CSV processor; matches lightweight-first preference.
- **Alternatives considered:** miller (not-yet-installed), pandas (heavyweight).
- **Risk/cost:** ~5MB wheel; MIT license; no system deps.
- **Confidence:** high

## Approval

- **Decision:**
- **Conditions:**
- **Date:**

## Install

- **Command run:** pip install --user csvkit
- **Verification:** exit=1 stderr_head='error: externally-managed-environment\n\n× This environment is externally managed\n╰─> To install Python packages system-wi'
- **Date:** 2026-04-23
```

**Assessment:** **PASS on the wire-in**, with a real-world install-failure modality (PEP 668) faithfully surfaced end-to-end. The pipeline:

1. ✅ Dispatched the correct installer (`install_pip_user` via `normalize_install_method('pip-user') → 'pip_user'`).
2. ✅ Ran the correct command (`pip install --user csvkit`).
3. ✅ Captured the real exit code (1) and stderr head.
4. ✅ Wrote the Install section atomically via the parse-modify-re-render path.
5. ✅ Auto-transitioned `approved → failed` based on the InstallResult.success flag.
6. ✅ `/health` counter reflects two transitions (pending→approved + approved→failed).
7. ✅ Total end-to-end latency: **0.92s**.

The "failure" here is informationally accurate — the operator reading the file sees exactly what went wrong and can address it (switch to pipx, use `--break-system-packages`, escalate manually). Operational-first discipline: the system surfaces reality rather than fabricating success. Real-world install-strategy design follows up as an Open Question (§3 above).

---

## Appendix C — Test-suite state at session close

- `pytest tests/ -m "not integration and not live_smoke"`: **528 passed, 19 deselected, 0 failures** (27-28s).
- 29 new tests since session start: +12 test_ingest_catalog.py, +4 TestApproveTriggersInstall, +4 TestValidatorRichContentDrift, +3 TestInstallNpxMcp, +5 normalizer parametrizations, +1 dispatcher-route test.
- 19 deselected tests are the `integration` (ChromaDB cold-start heavy) and `live_smoke` (real Anthropic) markers — out-of-scope for the fast CI suite per existing discipline.

---

## Appendix D — Files touched this session

**Schema / DB / Alembic:**
- `alembic.ini` (new, generated + URL comment)
- `alembic/env.py` (new, written)
- `alembic/versions/4ff5b5898f71_baseline_existing_schema.py` (new, autogen-empty baseline)
- `alembic/versions/7bdb4433afa8_add_tool_type_enum_and_backfill.py` (new, autogen + hand-added backfill op.execute)
- `core/db/models.py` (added TOOL_TYPE_VALUES + tool_type column + Enum import)
- `core/db/session.py` (new `ensure_schema_current`; `init_db` docstring update)
- `core/app.py` (lifespan call swap)
- `pyproject.toml` (alembic dep)

**API surface:**
- `core/api/schemas.py` (tool_type field on ToolOut)
- `core/api/tools.py` (tool_type mapper + filter)
- `core/api/recommend.py` (CatalogToolView passes tool_type + install_method)

**Recommendation pipeline:**
- `core/recommend/schemas.py` (three new fields on ToolRecommendation)
- `core/recommend/prompt.py` (CatalogToolView surface + render + JSON_OUTPUT_ENVELOPE)
- `core/recommend/validator.py` (new rich-content check + integration)

**Ingest:**
- `core/ingest/catalog.py` (new, 210 lines)

**Lifecycle store + install:**
- `core/lifecycle_store/service.py` (install_dispatcher DI + `_maybe_install_on_approve` hook + InstallResult import)
- `core/lifecycle_store/writer.py` (new `update_install_section` + parse_request_file import)
- `core/install/methods.py` (new `install_npx_mcp` + `METHOD_NPX_MCP` + `DEFAULT_NPX_MCP_TIMEOUT`)
- `core/install/service.py` (npx_mcp normalizer signal + dispatch route + INSTALL method import)
- `core/install/__init__.py` (install_npx_mcp in `__all__`)

**Tests:**
- `tests/test_ingest_catalog.py` (new, 12 tests)
- `tests/test_recommend_validator.py` (updated fixture + new TestValidatorRichContentDrift)
- `tests/test_lifecycle_service.py` (fixture noop + new TestApproveTriggersInstall)
- `tests/test_requests_api.py` (fixture noop)
- `tests/test_smoke_roundtrip.py` (fixture noop)
- `tests/test_install.py` (new TestInstallNpxMcp + npx normalizer params + dispatcher route)

**Planning:**
- `planning/decisions/DECISIONS.md` (three new entries appended)
- `planning/sessions/SESSION-2026-04-24-01.md` (this file, new)
- `planning/today.md` (Fix Day 2 regeneration — written next, see closeout sequence)

**Scripts:**
- `scripts/verify_denial_recall.py` (new, 150 lines, gated by CONCIERGE_LIVE_SMOKE=1)

Total: 11 new files, ~15 edited files. One live uvicorn restart (mid-session, after API edits). One real DB migration applied. One real Opus call (denial-recall). One real subprocess install attempt (csvkit). Zero regressions.
