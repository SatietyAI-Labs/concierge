# Session SESSION-2026-04-25-01

**Started:** 2026-04-24 ~17:45 PDT (Fix Day 2 opener pasted; logical-day framing 2026-04-25 per close-the-gap plan and today.md — Fix Day 2 ran earlier than planned, same shape as Fix Day 1's day-shift)
**Ended:** 2026-04-24 ~20:30 PDT
**Primary goal:** Fix Day 2 — catalog ingests skills as fourth peer category; tool-lifecycle state machine schema in place; usage-log table accepting writes; Task 0 migration-drift integration test as preflight.
**Goal achieved:** Yes — all six Fix Day 2 checkpoint criteria met. Task 0 caught a real Fix Day 1 Alembic bug (empty baseline migration) on its first run, which was fixed mid-stream. All downstream migrations applied clean on a live DB stamped at the rewritten baseline. Skills ingest verified end-to-end against a synthetic fixture.

## What was attempted this session

Standard open-phase cadence: seven opener documents read, three `[2026-04-24 Fix Day 1]` DECISIONS confirmed at tail, six proposed defaults on task ambiguities surfaced (drift-comparator granularity, skills-ingest source-path policy, skill slug collisions, lifecycle_state backfill mapping, ambient_loading null semantics for non-skill rows, event_type enum vs free-form string). Lewie ack'd with "ready" — implicit judgment authorization per memory `feedback_single_block_multi_task_days`. Single-block execution, mid-stream architectural pause surfaced on Task 0 when the drift test caught a real production bug.

Session progressed Task 0 → Task 3 → Task 4 → Task 2 → Task 1 → Task 5 as proposed. One mid-stream architectural call: rewriting the empty Fix Day 1 baseline migration to produce correct behavior on fresh-clone `alembic upgrade head` (paused, surfaced Option A/B/C/D to Lewie, greenlit Option A, executed with full-suite regression guard). Secondary fix emerged from the same chain — `alembic/env.py`'s default `fileConfig` call was disabling the `concierge` logger, silently breaking 18 caplog-based tests after the drift test ran; added `disable_existing_loggers=False` to preserve scope.

Live verification: skills ingest ran against `/tmp/concierge-skills-demo/{public,user,examples}` with 6 synthetic SKILL.md files, populated 6 skill rows in the live DB (verified via direct SQL query), demonstrated `tool_type=skill` rendering in the catalog-view payload. Tmp fixture cleaned up + stale skill rows removed from live DB before session close.

## What was completed

- **Task 0 — Alembic migration-drift integration test** — new `tests/test_alembic_drift.py` (150 lines, 2 tests). `test_alembic_matches_metadata_create_all` spins a tmp SQLite, runs `alembic upgrade head` via `alembic.command.upgrade`, reflects the result into a normalized `{columns, indexes, fks}` dict, compares against `Base.metadata.create_all()` reflected the same way. `test_alembic_drift_detector_catches_injected_column` is a meta-test — injects a phantom column into the metadata-path dict and confirms the comparator rejects. Both green.
- **Baseline migration fix** — rewrote `alembic/versions/4ff5b5898f71_baseline_existing_schema.py` from zero-ops stub into a real `op.create_table` baseline with `has_table("tools")` short-circuit so stamped-forward DBs stay a no-op while fresh installs get the four core tables (packs / tools / requests / memory_events, all at their pre-`tool_type` shape).
- **env.py logger preservation** — `alembic/env.py:33` `fileConfig(config.config_file_name, disable_existing_loggers=False)`. Documents the test-order side effect in the comment.
- **Task 3 — `lifecycle_state` column + backfill** — `core/db/models.py:23-38` new `LIFECYCLE_STATE_VALUES` tuple (`discovered` / `pending` / `used` / `loaded-on-boot` / `retired`); `Tool.lifecycle_state` Enum column with `server_default='discovered'` + index. Migration `2fe7a135d9dd` with backfill mapping: `is_active=True, is_in_manifest=True → 'loaded-on-boot'`; `is_active=True, is_in_manifest=False → 'used'`; everything else defaults to `'discovered'`. Live DB: 48 rows backfilled (34 `loaded-on-boot` + 14 `discovered`), zero NULLs. Two model-layer unit tests.
- **Task 4 — `ToolUsageEvent` usage-log table** — `core/db/models.py:41-56` new `USAGE_EVENT_TYPE_VALUES` tuple (`recommended` / `installed` / `loaded` / `used` / `removed`); new `ToolUsageEvent` model with `tool_id` FK to `tools.id`, `event_type` Enum column with CHECK constraint, `timestamp` server_default=now, `session_id` nullable, `context` JSON nullable. Migration `7d29e49d52c5`. Three model-layer unit tests (roundtrip, all-five-types accepted, nullable session/context).
- **Task 2 — `path` + `ambient_loading` columns** — `core/db/models.py:108-119` added both as nullable Optional fields. Migration `50364025e26b`. No backfill needed — existing rows are all non-skill categories where NULL is correct.
- **Task 1 — skills ingest path** — new `core/ingest/skills.py` (220 lines). Walks `<skills_root>/{public,user,examples}` with deterministic subdir order; parses YAML-style SKILL.md frontmatter (BOM-tolerant, continuation-line-aware); idempotent upsert by slug with first-seen-wins on cross-subdir collisions; missing root / subdirs log WARN and return zero-ingested (fresh clones don't crash). New `Settings.skills_root: Path = Path("/mnt/skills")` with `CONCIERGE_SKILLS_ROOT` env override. 14 unit tests in `tests/test_ingest_skills.py` + 1 real-source smoke (skipped on this WSL box where `/mnt/skills/` doesn't exist — logged for future Claude.ai-hosted soak run).
- **ToolOut schema extension** — `core/api/schemas.py:21-23` adds `lifecycle_state: str`, `path: Optional[str]`, `ambient_loading: Optional[bool]`. `core/api/tools.py::_to_out` passes them through. Two new API tests: `test_list_tools_filter_by_skill_tool_type_surfaces_path_and_ambient_loading` and `test_list_tools_non_skill_rows_report_null_path_and_ambient_loading`.
- **Task 5 — catalog-ingest rendering enrichment for skills** — `CatalogToolView` gains `path` + `ambient_loading`; `_render_catalog` dispatches by `tool_type == "skill"` to a new `_render_skill_row` that tags `<skill>` + `[ambient]` or `[on-demand]` + surfaces `path=<SKILL.md>`, and omits the `install=<method>` annotation (skills are ambient, not installed). `core/api/recommend.py::_catalog_view` passes the two new fields through. Six new tests in `TestSkillsCatalogRendering` covering the render branch, ambient/on-demand distinction, mixed-category rendering, and no-install invariant.
- **Live end-to-end skills-ingest verification** — ran `CONCIERGE_SKILLS_ROOT=/tmp/concierge-skills-demo python -m core.ingest.skills` against a synthetic fixture populating `public/{claude-api,loop,schedule,simplify,update-config}/SKILL.md` and `user/my-scratch/SKILL.md`. Output: "Skills: +6 created, ~0 updated". Direct SQL verified: 6 rows with `tool_type=skill`, `ambient_loading=1`, `path` set to resolved absolute paths, `lifecycle_state=discovered`. Tmp fixture removed + stale rows deleted from live DB before session close.
- **One new DECISIONS entry** — `[2026-04-25 Fix Day 2]` — Alembic baseline migration made fresh-install-safe via `has_table` guard. Appended to `planning/decisions/DECISIONS.md`.
- **Tests:** 530 at session start (post Task 0 landing) → **557 passing, 1 skipped, 19 deselected** at session close. +27 new tests across 4 files, zero regressions.

## What was started but not finished

Nothing mid-flight. All six tasks complete with tests + live verification. Deferred items are explicit and expected:

- **Real-source skills smoke** — `test_ingest_skills_real_source_smoke` skips on this WSL box because `/mnt/skills/` isn't present. Will exercise automatically the first time Concierge runs in a Claude.ai-hosted harness where the path exists, or when the operator sets `CONCIERGE_SKILLS_ROOT` to a dir populated in the three-subdir layout. Not a gap — it's the documented fallback.
- **Tool-transition validation** (Fix Day 3) — `lifecycle_state` writes today are bare SQLAlchemy `setattr` / `tool.lifecycle_state = X`. Validation lives in Fix Day 3's Task 1 per the close-the-gap plan.
- **Usage-telemetry emit hooks** (Fix Day 3) — the table exists and accepts writes; `concierge_recommend` / `install_by_method` / loader `load()` emit code is Fix Day 3.
- **PEP-668 install-strategy design** (Fix Day 1 open question) — still pending Lewie's ruling on pipx vs system-pip3 vs escalation. Didn't touch the install methods today per today.md's "What Fix Day 2 is NOT."

## Decisions made this session

One, appended to DECISIONS.md:

- **Alembic baseline migration made fresh-install-safe via `has_table` guard** — see DECISIONS.md `[2026-04-25 Fix Day 2]`. Covers both the main fix (rewritten baseline migration) and the secondary `disable_existing_loggers=False` fix on `alembic/env.py`.

The six proposed defaults on task ambiguities (drift-comparator granularity, skills-ingest source policy, skill slug collisions, lifecycle_state backfill mapping, `ambient_loading` null semantics, `event_type` enum) were mentioned at session open and executed under implicit judgment authorization. They're documented inline in the relevant migrations / modules, not as separate DECISIONS entries, per the "routine implementation choices don't get logged" rule — the mapping table lives in the Task 3 migration docstring; the first-seen-wins collision policy lives in the skills module docstring; etc.

## Bugs / issues discovered

**One structural bug — already fixed.** The Fix Day 1 Alembic baseline (`4ff5b5898f71_baseline_existing_schema.py`) was a zero-ops stub. Autogenerated against the already-populated dev DB, then `alembic stamp head`'d. Fresh-clone `uvicorn core.app:app` → `ensure_schema_current()` → `alembic upgrade head` reached revision `7bdb4433afa8` and crashed at `ALTER TABLE tools ADD COLUMN tool_type` with `no such table: tools`. This was exactly the drift the Task 0 test was designed to catch; caught on first run. Fix logged in DECISIONS `[2026-04-25 Fix Day 2]`.

**One test-ordering side effect — already fixed.** `alembic/env.py` called `fileConfig(config.config_file_name)` with `disable_existing_loggers=True` (the default), which disabled the `concierge` logger after Alembic was invoked programmatically in any test. 18 caplog-based tests failed in full-suite runs because their target logger had been silenced. Fix: `disable_existing_loggers=False` on fileConfig. Documented in the env.py comment.

## Open questions for Lewie

1. **PEP-668 install-strategy design** — still outstanding from Fix Day 1. No change today; operational-first says honest failure surfacing is correct until a strategy is chosen. A pipx-based `install_pip_user` swap is the most defensible forward move when you're ready.

2. **Real-source skills smoke** — is it worth adding a CI-gated cron that exercises the real-source test weekly via a live Claude.ai-hosted harness, or is one-time verification sufficient? Default if no answer: one-time verification during soak + spot-check monthly. The `test_ingest_skills_real_source_smoke` test is already structured to pass automatically whenever `skills_root` is populated, so no extra infrastructure needed.

3. **Skills recommender-prompt narration** — should the JSON output envelope explicitly instruct Opus to mention "ambient skill available" when a skill is the top recommendation? Currently the `[ambient]` render-tag is visible but the envelope doesn't reference it. Default if no answer: defer to Fix Day 4's narration-as-push work where the envelope + MCP tool descriptions get a coordinated overhaul anyway.

## Next session should start by

Open a fresh Claude Code session tomorrow morning (2026-04-25 calendar / logical Fix Day 3). Paste the opener from the to-be-written `planning/today.md` §Session opener. First concrete action: Task 1 from the Fix Day 3 plan — new `core/tool_transitions.py` with the legal-transition table mirroring `core/lifecycle_store/transitions.py` pattern. That's the Fix Day 3 foundational task; Task 1 unblocks Tasks 2-7 (usage telemetry emit hooks, loader unload/list_active, identity notes).

`/health` state at close: `tools: 48, packs: 8, tool_usage_events: 0`. Alembic rev: `50364025e26b`. Uvicorn restart not needed for Fix Day 3 kickoff (none of today's edits affect a running server — they're schema-level; the `_render_catalog` + `ToolOut` changes take effect on the next FastAPI process start).

## Context the next session needs to read first

1. `planning/today.md` — Fix Day 3 plan with opener
2. `docs/close-the-gap-plan-2026-04-23.md` §Fix Day 3 section
3. `planning/sessions/SESSION-2026-04-25-01.md` — this snapshot
4. `planning/decisions/DECISIONS.md` tail — `[2026-04-25 Fix Day 2]` entry
5. `core/db/models.py` — updated Tool model + new `ToolUsageEvent`; `LIFECYCLE_STATE_VALUES` + `USAGE_EVENT_TYPE_VALUES` constants feed Fix Day 3's transition validation + emit hooks

---

## Appendix A — Live skills-ingest verification transcript

Synthesized fixture (bash):

```bash
mkdir -p /tmp/concierge-skills-demo/{public,user,examples}
for skill in update-config simplify loop schedule claude-api; do
  d="/tmp/concierge-skills-demo/public/$skill"
  mkdir -p "$d"
  printf -- "---\nname: %s\ndescription: Demo skill used for Fix Day 2 live verification.\n---\n\n# %s\n" "$skill" "$skill" > "$d/SKILL.md"
done
mkdir -p /tmp/concierge-skills-demo/user/my-scratch
printf -- "---\nname: my-scratch\ndescription: Operator-local skill.\n---\n" > /tmp/concierge-skills-demo/user/my-scratch/SKILL.md
```

Ingest invocation + output:

```
$ CONCIERGE_SKILLS_ROOT=/tmp/concierge-skills-demo python -m core.ingest.skills
Ingested skills from: /tmp/concierge-skills-demo
  Subdirs walked: 3 / missing: 0
  Skills: +6 created, ~0 updated
  Skipped (collisions): 0
```

Live DB state post-ingest:

```
=== tool_type distribution ===
  cli      32
  http     7
  mcp      9
  skill    6   ← fourth peer category active

=== skill rows ===
  claude-api         ambient=1 state=discovered  path=/tmp/concierge-skills-demo/public/claude-api/SKILL.md
  loop               ambient=1 state=discovered  path=/tmp/concierge-skills-demo/public/loop/SKILL.md
  my-scratch         ambient=1 state=discovered  path=/tmp/concierge-skills-demo/user/my-scratch/SKILL.md
  schedule           ambient=1 state=discovered  path=/tmp/concierge-skills-demo/public/schedule/SKILL.md
  simplify           ambient=1 state=discovered  path=/tmp/concierge-skills-demo/public/simplify/SKILL.md
  update-config      ambient=1 state=discovered  path=/tmp/concierge-skills-demo/public/update-config/SKILL.md
```

Cleanup: tmp fixture `rm -rf`'d; stale skill rows DELETEd from live DB via direct SQL (paths matched `/tmp/concierge-skills-demo/%`). Final catalog distribution: cli=32 / mcp=9 / http=7 / skill=0, matching pre-verification state.

---

## Appendix B — Test-suite state at session close

- `pytest tests/ -m "not integration and not live_smoke"`: **557 passed, 1 skipped, 19 deselected, 0 failures** (54s).
- 27 new tests since session start (530 → 557):
  - +2 `tests/test_alembic_drift.py` (Task 0)
  - +2 `tests/test_db.py::test_tool_lifecycle_state_*` (Task 3)
  - +3 `tests/test_db.py::test_tool_usage_event_*` (Task 4)
  - +14 `tests/test_ingest_skills.py::test_*` (Task 1 — 1 real-source smoke marked skip)
  - +2 `tests/test_api_tools.py::test_list_tools_*` (Task 1 API coverage)
  - +6 `tests/test_recommend_prompt.py::TestSkillsCatalogRendering` (Task 5)
- 1 skipped test = `test_ingest_skills_real_source_smoke` (expected — no `/mnt/skills/` on WSL).
- 19 deselected = integration + live_smoke markers (unchanged from Fix Day 1 baseline).

---

## Appendix C — Files touched this session

**Schema / DB / Alembic:**
- `alembic/env.py` (`disable_existing_loggers=False`)
- `alembic/versions/4ff5b5898f71_baseline_existing_schema.py` (rewritten from zero-ops to full baseline with has_table guard)
- `alembic/versions/2fe7a135d9dd_add_lifecycle_state_column_with_backfill.py` (new — Task 3)
- `alembic/versions/7d29e49d52c5_add_tool_usage_events_table.py` (new — Task 4)
- `alembic/versions/50364025e26b_add_tools_path_and_tools_ambient_loading.py` (new — Task 2)
- `core/db/models.py` (added `LIFECYCLE_STATE_VALUES`, `USAGE_EVENT_TYPE_VALUES` constants; `Tool.lifecycle_state` / `path` / `ambient_loading` columns; new `ToolUsageEvent` model)
- `core/config.py` (new `skills_root` setting)

**API surface:**
- `core/api/schemas.py` (ToolOut fields: `lifecycle_state`, `path`, `ambient_loading`)
- `core/api/tools.py` (`_to_out` passes through new fields)
- `core/api/recommend.py` (`_catalog_view` passes `path` + `ambient_loading`)

**Recommendation rendering:**
- `core/recommend/prompt.py` (`CatalogToolView` fields; `_render_catalog` dispatches `tool_type=='skill'` → new `_render_skill_row`; new `_render_standard_row` for the prior MCP/CLI/HTTP path)

**Ingest:**
- `core/ingest/skills.py` (new, 220 lines — frontmatter parser + iter_skill_rows + ingest_skills + CLI main)

**Tests:**
- `tests/test_alembic_drift.py` (new — Task 0)
- `tests/test_db.py` (added `test_tool_lifecycle_state_*` x2, `test_tool_usage_event_*` x3)
- `tests/test_ingest_skills.py` (new, 14 tests + 1 real-source smoke)
- `tests/test_api_tools.py` (+2 tests)
- `tests/test_recommend_prompt.py` (+6 tests in `TestSkillsCatalogRendering`)

**Planning:**
- `planning/decisions/DECISIONS.md` (one new `[2026-04-25 Fix Day 2]` entry appended)
- `planning/sessions/SESSION-2026-04-25-01.md` (this file, new)
- `planning/today.md` (Fix Day 3 regeneration — written next, see closeout sequence)

Total: 10 new files, ~12 edited files. Three real Alembic migrations applied to the live DB in sequence. One migration rewrite after drift-test exposure. One real end-to-end skills-ingest run with verified DB state + cleanup. Zero regressions.
